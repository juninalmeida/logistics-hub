# Configura e sobe o serviço de banco de dados (PostgreSQL) isolado usando Docker
services:
  postgres:
    image: "bitnami/postgresql:latest"
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: logistics-hub
